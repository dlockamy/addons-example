jenkins:
  systemMessage: "Jenkins configured with JCasC running in Docker!"
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "${ADMIN_PASSWORD}" # Use environment variable for secrets
  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false
